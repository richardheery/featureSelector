type(mtcars)
data = mtcars
class(data)
data = as.data.frame(data)
class(data)
data
# Find constant features
constant_features = names(which(sapply(ddata, function(x) length(unique(x))) == 1))
# Find constant features
constant_features = names(which(sapply(data, function(x) length(unique(x))) == 1))
constant_features
?as
# Find constant features and remove them
constant_features = names(which(sapply(data, function(x) length(unique(x))) > 1))
# Find constant features and remove them
constant_features = names(which(sapply(data, function(x) length(unique(x))) == 1))
constant_features
paste("Removing", length(constant_features), "constant features:")
constant_features
constant_features = head(names(mtcars))
constant_features
dplyr::select(data, !constant_features)
dplyr::select(data, !dplyr::all_of(constant_features))
head(dplyr::select(data, !dplyr::all_of(constant_features)))
head(dplyr::select(data, dplyr::all_of(constant_features)))
message(paste("Removed", length(constant_features), "constant features"))
data
data$mpg2 = data$mpg
data = as.data.frame(data)
# Find duplicated features
duplicated_features = names(df)[duplicated(as.list(df))]
duplicated_features
# Find duplicated features
duplicated_features = names(data)[duplicated(as.list(data))]
duplicated_features
# Remove duplicated features
data = dplyr::select(data, !dplyr::all_of(duplicated_features))
message(paste("Removed", length(duplicated_features), "duplicated features."))
